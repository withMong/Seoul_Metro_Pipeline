"""노선 특수 규칙 — 참조 목록(실데이터에 맞춰 보완 가능).

#3 피드백 반영:
  - 2호선은 순환선 + 지선(성수·신정)이라 '연속 두 열차'가 섞이면 안 됨 → 지선 역을 라벨.
    (헤드웨이는 본래 역×방향 단위라 지선/본선이 역으로 구분되지만, 명시 라벨로 필터 가능하게.)
  - 1호선은 서울 외(코레일) 구간 미제공 → 경계역에서 열차가 사라지는 건 '정상 소멸'.
    그 역의 큰 공백은 결측(gap_missing)이 아니라 정상 종료(normal_term)로 분류.
"""

# 2호선 지선 역(statn_nm) — 성수지선 / 신정지선
BRANCH_2 = [
    "성수지선", "용답", "신답", "용두", "신설동",          # 성수지선
    "신정지선", "신도림지선", "도림천", "양천구청", "신정네거리", "까치산",  # 신정지선
]

# 1호선 정상 소멸(서울 구간 경계) 역 — 여기서 사라지면 코레일 구간 이탈(정상)
EDGE_1 = [
    "서울역", "청량리", "광운대", "구로", "도봉산",
    "인천", "신창", "서동탄", "광명", "병점",
]


def sql_in(names) -> str:
    """SQL IN (...) 리스트 문자열."""
    return ",".join("'" + n.replace("'", "''") + "'" for n in names)
